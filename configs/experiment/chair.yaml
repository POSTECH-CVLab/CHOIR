# @package _global_

# Single-class: chair
# Usage: uv run python src/train.py experiment=chair

defaults:
  - override /data: shapenet
  - override /model: choir
  - override /trainer: ddp

data:
  label_in_use: "4"

trainer:
  max_epochs: 3000
